{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**"],
      "env": ["NEXT_PUBLIC_SITE_URL"]
    },
    "lint": {
      "outputs": []
    }
  }
}
